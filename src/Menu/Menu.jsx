import React from 'react';
import PropTypes from 'prop-types';

function Menu(props) {
	return (
		<div>
			Hello world!
		</div>
	);
}

Menu.propTypes = {
};

Menu.defaultProps = {
};

export default Menu;
